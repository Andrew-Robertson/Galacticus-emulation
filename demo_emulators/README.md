# Demo Emulators

This directory contains curated saved emulator artifacts used by the interactive
slider demos. The matching browser UI files live under `assets/interactive_*_demo/`.

Use `playing/` for scratch training outputs, smoke-test artifacts, and candidate
emulators that are not meant to be loaded by default.
