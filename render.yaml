services:
  - type: web
    name: galacticus-emu-demo
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: python scripts/serve_interactive_fastapi_demo.py --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
